{
  "routes": [
    {
      "method": "GET",
      "path": "/slots",
      "handler": "slots.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/slots/count",
      "handler": "slots.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/slots/:id",
      "handler": "slots.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/slots",
      "handler": "slots.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/slots/:id",
      "handler": "slots.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/slots/:id",
      "handler": "slots.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
